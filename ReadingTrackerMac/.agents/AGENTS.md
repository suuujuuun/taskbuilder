# General Guidelines for Taskbuilder macOS App

- **App Theme**: The app theme is black. Use a dark mode aesthetic where button colors and other UI elements rely on subtle contrast differences between black and gray. Elements should be unobtrusive and not overly flashy.

but there's few exeptions exist so don't delete it if there color is not black or gray 
- **Language**: The app's primary language is English.
- **Development Philosophy**: NEVER delete existing features. Development must always be additive (building upon existing features).
- **Data Stability**: Data stability and safety are of utmost importance. Ensure data is properly persisted and protected.
- when import export it has to put everydata don't forget to design it as json format. and when you import it has to check if it already exists
